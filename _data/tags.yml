# Add an entry, where the key represents a slug.
# Provide at least a `name` value and optionally `image`, `color` and `description`.
# Markdown is enabled for `description`, however don't use more than one paragraph (enforced by `>`)
#
# Example:
#
# ~~~
# mytag:
#   name: My Tag
#   description: >
#     *My Tag* is an example of how to define a custom tag.
# ~~~

hyde:
  name: python
  image: /hydejack/public/img/hyde.jpg
  color: '#949667'
  description: >
    python is a brazen two-column Jekyll theme that pairs a prominent sidebar with uncomplicated content.
    It’s based on Poole, the Jekyll butler.

django:
  name: django
  image: /hydejack/public/img/jekyll.jpg
  color: '#949667'
  description: >
    django 
